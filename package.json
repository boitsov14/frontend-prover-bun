{
  "scripts": {
    "dev": "concurrently \"bun build --experimental-html --experimental-css src/index.html --outdir=dist --watch\" \"reload --browser --dir dist\"",
    "check": "bun biome check --write src",
    "bundle": "bun build --experimental-html --experimental-css src/index.html --outdir=dist"
  },
  "dependencies": {
    "@panzoom/panzoom": "latest",
    "alpinejs": "latest",
    "katex": "latest",
    "ky": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@tsconfig/bun": "latest",
    "@tsconfig/strictest": "latest",
    "@types/alpinejs": "latest",
    "@types/bun": "latest",
    "@types/katex": "latest",
    "concurrently": "latest",
    "reload": "latest"
  }
}
