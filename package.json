{
  "scripts": {
    "dev": "bun build --experimental-html --experimental-css src/index.html --outdir=dist --watch",
    "check": "bun biome check --write src",
    "bundle": "bun build --experimental-html --experimental-css src/index.html --outdir=dist"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@tsconfig/bun": "latest",
    "@tsconfig/strictest": "latest",
    "@types/bun": "latest"
  }
}
